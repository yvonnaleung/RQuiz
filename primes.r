is.prime <- function(n) {
  # TODO: return whether n is prime.
  # Prime number is only divisible by one and itself.
  # By convention, 1 is not prime.
  # It is sufficient to check that all numbers between 2 and the square root
  # of n (inclusive) do not divide n.
  
  FALSE
}

primes.less.than <- function(n) {
  # TODO: return a vector of all the primes less than n.
  c(n)
}